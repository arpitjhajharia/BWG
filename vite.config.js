import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: "/BWG/", // <--- ADD THIS LINE (Must match your Repo name exactly)
})
